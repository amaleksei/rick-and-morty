import styles from '../../styles/Footer.module.css'

export default function Footer() {
  return (
    <footer className={`${styles.footer} flex items-center justify-center px-16 w-full`}>
      <p>© 2022</p>
    </footer>
  )
}
